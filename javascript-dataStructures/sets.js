const ids = new Set();
 // or

 const id_two = new Set([1,'abc',2, '3', 'abd']);
 
 ids.add('abc');   
 ids.add(1);
 ids.add('bca');
 ids.add(2);

 for(item of ids) {
    ids
 }

 //console.log(ids.has('abc'))


